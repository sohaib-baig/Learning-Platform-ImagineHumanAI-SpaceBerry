# Firebase client (from Firebase console)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# (Optional) Firebase service account for admin tasks (used server-side only if needed)
FIREBASE_SERVICE_KEY_BASE64=  # base64-encoded JSON (optional for custom claims ops)

# Mux (for playback; MVP uses pre-existing playback IDs)
NEXT_PUBLIC_MUX_ENV_KEY=      # not required for playback-only; use @mux/mux-player-react with playbackId

# Postmark
POSTMARK_SERVER_TOKEN=
POSTMARK_FROM_EMAIL=hello@imaginehumans.ai

# Amplitude
NEXT_PUBLIC_AMPLITUDE_API_KEY=
